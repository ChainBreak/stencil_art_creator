[project]
name = "stencil-art-creator"
version = "0.1.0"
description = "Converts images into stencil cutouts"
requires-python = ">=3.11"
dependencies = [
    "gradio",
    "numpy",
    "click",
    "pillow",
    "opencv-python",
]
